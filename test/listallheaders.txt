    buffer << "Source IP" << endl;
    for (const auto& p : src_ip) {
        buffer << ip_int_to_string(p.first) << " " << p.second << std::endl; 
    }
    buffer << "Destination IP" << endl;
    for (const auto& p : dst_ip) {
        buffer << ip_int_to_string(p.first) << " " << p.second << std::endl; 
    }

    buffer << "Source Port" << endl;
    for (const auto& p : src_port){
        buffer << p.first << " " << p.second << std::endl; 
    }

    buffer << "Destination Port" << endl;
    for (const auto& p : dst_port){
        buffer << p.first << " " << p.second << std::endl; 
    }

    buffer << "Protocol" << endl;
    for (const auto& p : protocol){
        buffer << get_protocol(p.first) << " " << get_protocol(p.second) << std::endl; 
    }

    buffer << "Flags" << endl;
    for (const auto& p : flags){
        buffer << get_flags(p.first) << " " << p.second << std::endl; 
    }